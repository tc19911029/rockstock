/**
 * /mockup currently mirrors the real homepage.
 *
 * Keep this route as a safe preview baseline. Future UI experiments should be
 * small, reviewable changes here before anything moves into production.
 */

export { default } from '../page';
